# Public 目录

此目录用于存放公共静态资源文件，这些文件会被直接复制到构建输出目录的根路径。

## 常见用途

- `favicon.ico` - 网站图标
- `robots.txt` - 搜索引擎爬虫配置
- 第三方库的 JS/CSS 文件
- 其他需要原样输出的静态资源

## 访问方式

放在 `public` 目录下的文件，可以通过根路径 `/` 直接访问。

例如：`public/logo.png` → 访问路径为 `/logo.png`
