# Mock 数据目录

此目录用于存放 Mock 数据相关的文件和模块。

## 目录结构

- `vue3-sub-app/` - Vue3 子应用的 Mock 数据
- `vue2-sub-app/` - Vue2 子应用的 Mock 数据
- `factories/` - Mock 数据工厂函数
- `data/` - 基础 Mock 数据
- `handlers/` - Mock 接口拦截器
